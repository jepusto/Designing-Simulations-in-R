\usepackage{booktabs}
\usepackage{amsthm}
\makeatletter
\def\thm@space@setup{%
  \thm@preskip=8pt plus 2pt minus 4pt
  \thm@postskip=\thm@preskip
}
\makeatother

\usepackage{color}
\usepackage{subfig}

% Exercise sub-parts (a), b), c), ...): keep them flush with the exercise
% text instead of the default enumerate indent, so they don't read as a
% nested bulleted sub-list.
\usepackage{enumitem}
\usepackage{etoolbox}
\AtBeginEnvironment{exercise}{%
  \setlist[enumerate]{leftmargin=1.5em, itemsep=2pt, topsep=4pt, parsep=0pt}
}

\usepackage{makeidx}
\makeindex


% Shortcodes for editing
\newcommand\cmnt[2]{\qquad{{\color{red} \em #1---#2} \qquad}}
\newcommand\cmntM[1]{\cmnt{#1}{Miratrix}}
\newcommand\awk{{{\color{red} {$\leftarrow$ Awkward phrasing}}\qquad}}
\newcommand\cmntMp[1]{{\color{red} $\leftarrow$ {\em #1 -Miratrix} \qquad}}

% Shortcodes for math and probability
\newcommand{\Prob}{\text{Pr}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\M}{\mathbb{M}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\Var}{\text{Var}}
\newcommand{\Bias}{\text{Bias}}
\newcommand{\RMSE}{\text{RMSE}}
\newcommand{\Cov}{\text{Cov}}
\newcommand{\cor}{\text{cor}}
\newcommand{\diag}{\text{diag}}
\newcommand{\mat}[1]{\mathbf{#1}}
\newcommand{\bs}{\boldsymbol}
\newcommand{\trace}{\text{tr}}


\usepackage{imakeidx}
\makeindex

% Redefine \and to a line break to avoid nested tabular calls in krantz.cls
% The krantz.cls redefines \@tabular/\endtabular using \crcr, which conflicts
% with the new LaTeX expl3 tabular backend when \and (which normally expands to
% \end{tabular}...\begin{tabular}) is used inside \@maketitle.
\renewcommand{\and}{\\}
